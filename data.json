let tradeData={};

async function loadData(){
  try{
    const res=await fetch("data.json?"+Date.now()); // cache-buster
    tradeData=await res.json();
    updateUI();
  }catch(e){
    console.error("Gagal load data.json:",e);
  }
}

loadData();
